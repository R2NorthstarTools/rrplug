/// this marco assumes the functions are the same sq function (on diffrent vms)
#[macro_export]
macro_rules! sq_function_group {
    ($value:expr, $sqvm:expr, $sq_functions: expr) => {
        todo!()
    };
}
